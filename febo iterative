#include <stdio.h>

int main()
{
   printf("enter the number\n");
   int n;
   scanf("%d",&n);
   int a=1,b=0,sum=0;
   for(int i=0;i<n;i++)
   {
       sum=a+b;
       printf("%d ",sum);
       a=b;
       b=sum;
   }

    return 0;
}
