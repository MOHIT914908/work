#include <stdio.h>
int feb(int n)
{
    if(n<=1)
     return 1;
    else
     return feb(n-1)+feb(n-2);
    
}
int main()
{
   printf("enter the number\n");
   int n;
   scanf("%d",&n);
   for(int i=0;i<n;i++)
   {
       printf("%d ",feb(i));
   }
    return 0;
}
